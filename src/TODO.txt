
main:
  - convert dst path and search path to absolute path
     - throw exception if abs path>MAX_PATH

path_utils
  - module for path operations

finder
  - realloc when needed instead of 1000000 files(NO)
  - create link each time a new file is find?
  - cleanup  finder_find_in_dir

IPC
  - remove listenning to SIG_INT

GLOBAL:
  - set create permissions globally (UMASK?) for all file/dir creation
  - put MAX_PATH globally
  - usage of system functions instead of C lib functions
     - opendir vs open & getdents
     - fopen vs open,etc
  - test malloc res for failure
  - add module prefix in private functions ?
  - perror(), right parameter?
  - test with VALGRIND

Modules to implement:
  - File linker
  - file_validator (noeuds: &, ||, leafs: test_name, test_date, test_size, test_etc)
  - deamonize


Implementation doc:
  - Describe singleton IPC
  - Describe callback/args

Diagram:
  - Remove params diagram
  - remove private fields from IPC
  - remove private functions from IPC & Finder
  - name 1st layer (Presentation Layer)
  - Main --- parse --> Parser
