GLOBAL:
 -  DOCUMENT FUNCTIONS
    - CLAUDIO: finder, smart_folder, validator, parser
    - DAVID: main, io, logger, ipc, linker

IPC:
  - if IPCwatch  cannot be set, free parser instance  DAVID
  - incoherence between binary name and folder created within $HOME. Use searchfolder everywhere DAVID
  - replace SIGNAL api DAVID

finder
  - if 100000 files found, stop searching for more !!! CLAUDIO
  - cleanup  finder_find_in_dir !! CLAUDIO
  - use io MAX_PATH and not local DAVID

Logger:
 - replace all err and log message to calls to this configurable module. INFO,ERROR, DEBUG (precompile constant) DAVID

smart_folder
 - if cannot be instanciated: free parser instance DAVID

Validator:
 - put 0b1111 in named constant: CLAUDIO

Tests:
 - put big file documenttation on top. comment frees no done

AT END:
  - usage of system functions instead of C lib functions !!!!!!
     - opendir vs open & getdents
     - fopen vs open,etc

