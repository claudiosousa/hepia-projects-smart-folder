#include "finder.h"

finder_t *finder_find(char *search_path, file_validator_t *validator)
{
    (void)search_path;
    (void)validator;

    return NULL;
}

void finder_free(finder_t *finder)
{
    (void)finder;
}
