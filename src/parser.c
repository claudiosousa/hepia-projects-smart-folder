#include "parser.h"

parser_t *parser_parse(char *expression[], size_t size)
{
    (void)expression;
    (void)size;

    return NULL;
}

void parser_free(parser_t * expression){
    (void)expression;
}
